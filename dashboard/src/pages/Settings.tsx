/**
 * Settings — frontend.md §3.4. Account, key, and notification preferences
 * land in later phases; this phase establishes the route and heading.
 */
export function Settings() {
  return (
    <div>
      <h1 className="font-display font-semibold text-display-lg text-text-primary">Settings</h1>
    </div>
  );
}
