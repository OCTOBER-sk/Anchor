/**
 * API reference — frontend.md §3.7. The endpoint and tool reference lands
 * in F3; this phase establishes the route and page heading inside the
 * DocsLayout.
 */
export function ApiReference() {
  return <h1 className="font-display font-semibold text-display-lg text-text-primary">API Reference</h1>;
}
