# Supabase project credentials — copy this file to .env and fill in real values.
# NEVER commit real values. The dashboard uses the anon key ONLY (never service_role).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
