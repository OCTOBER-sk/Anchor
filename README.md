# Anchor

The memory layer for your AI agents. Remote MCP server on Cloudflare Workers — Search, Dev Search, Memory behind one key, one endpoint.

- `backend.md` — full backend engineering spec (greenfield, free-tier only)
