## What does this PR do?

<!-- One paragraph. What changed and why. -->

## Related issue

<!-- Link the issue, e.g. #12. "None" if no issue. -->

## Verification

- [ ] `cd worker && npm test` passes
- [ ] `cd worker && npx tsc --noEmit` passes
- [ ] `cd dashboard && npm run typecheck` passes
- [ ] `cd dashboard && npm run build` passes
- [ ] No unrelated changes in the diff

## Copy discipline

- [ ] User-facing text contains no tech-stack names, no hype words, no "beta"/"working" phrasing

<!--
Before requesting review: re-read your diff. Every line should serve this PR's
single goal. If you found a bug while in there, open a separate issue for it.
-->
